Put your real project screenshots in this folder.

Each project in src/data/projects.js has an `image` field like:
  image: "/projects/ai-lms.png"

That path maps directly to a file in this folder (public/projects/).
Until you add a matching image file, the project card automatically
shows a generated placeholder graphic instead of a broken image.
